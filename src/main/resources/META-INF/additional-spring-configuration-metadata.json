{
  "properties": [
    {
      "name": "websocket.allowed-origins",
      "type": "java.lang.String[]",
      "description": "WebSocket STOMP 엔드포인트에 허용할 오리진 목록 (CORS). 쉼표로 구분하거나 배열로 지정. 프로덕션에서는 반드시 실제 도메인으로 설정하세요.",
      "defaultValue": "http://localhost:3000,http://localhost:5173"
    }
  ]
}
